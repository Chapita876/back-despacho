apiVersion: apps/v1
kind: Deployment
metadata:
  name: back-despacho
spec:
  replicas: 2
  selector:
    matchLabels:
      app: back-despacho
  template:
    metadata:
      labels:
        app: back-despacho
    spec:
      containers:
        - name: back-despacho
          image: christian98756/back-despacho:latest
          ports:
            - containerPort: 8080
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "test"
          resources:
            requests:
              cpu: "200m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: back-despacho-service
spec:
  type: ClusterIP
  selector:
    app: back-despacho
  ports:
    - port: 8080
      targetPort: 8080